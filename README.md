# February 14th — A Special Delivery

A Wes Anderson–inspired Valentine's page with an interactive envelope. Click to open and discover the surprise inside.

## Run locally

```bash
npm install
npm run dev
```

Open http://localhost:5173

## Build for production

```bash
npm run build
```

The static files will be in the `dist/` folder—ready to deploy anywhere.
# valentine-envelope
